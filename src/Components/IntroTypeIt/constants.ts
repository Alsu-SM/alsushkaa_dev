import { Options } from 'typeit';

export const HEART = '<3 ';

export const PAUSE = 2750;

export const OPTIONS: Options = {
	strings: ' ',
	speed: 200,
	deleteSpeed: 200,
	waitUntilVisible: false,
	lifeLike: true,
	loop: true,
	loopDelay: 2000,
	startDelay: 1000,
};
