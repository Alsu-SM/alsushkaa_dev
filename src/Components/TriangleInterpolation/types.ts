export type TriangleInterpolationProps = {
	sides: number;
	className?: string;
};

export type UseInterpolationProps = {
	sides: number;
	canvas: HTMLCanvasElement | null;
	canvasStatic: HTMLCanvasElement | null;
};
