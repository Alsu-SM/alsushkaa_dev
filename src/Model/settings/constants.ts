import { Language, Settings } from './types';

export const LANGUAGE_DEFAULT: Language = Language.EN;

export const SETTINGS_DEFAULT: Settings = {
	language: LANGUAGE_DEFAULT,
};

export const SETTINGS_LANGUAGE_STORAGE_KEY = 'SETTINGS_LANGUAGE_STORAGE_KEY';
